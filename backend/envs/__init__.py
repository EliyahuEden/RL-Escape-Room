"""Room environments.

The implementations live in :mod:`rl.envs` (single source of truth, also
used by the Streamlit fallback UI).  These modules re-export them under
the backend package layout.
"""
